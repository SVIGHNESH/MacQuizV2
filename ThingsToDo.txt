1. DONE - Results release policy + results views (backend and web player). Both the release-gated API and the web results UI are shipped.
2. DONE - The web attempt player, My quizzes workspace, and released-results review. Proven by web/e2e/player.e2e.mjs.
3. DONE - the Redis snapshot cache for attempt start (docs/01 "Go-live herd"). attempt.SnapshotCache (server/internal/attempt/snapshotcache.go) is a best-effort read-through cache keyed by (quiz_id, quiz_version) - immutable once a quiz_versions row is inserted, so a cache entry never goes stale. buildDetail (used by both Start and Get) now reads title/now() live from quizzes but resolves questions/guardrails through the cache first, falling back to Postgres only on a miss and populating it for the next reader. realtime.SnapshotCache (server/internal/realtime/snapshotcache.go) is the concrete Redis-backed implementation, wired in main.go alongside the existing publisher/subscriber with the same "an unreachable Redis degrades to a miss, never a boot or request failure" contract. Proven by server/internal/attempt/snapshotcacheflow_test.go (start populates the cache once; resume and a plain Get both hit it and never call Set again, while still returning the correct snapshot) and server/internal/realtime/snapshotcache_test.go (live Redis round trip). Full Go test suite passes with no regressions.

- Milestone 5 - Realtime (FR-7): the roster snapshot, attempt_events source-of-truth log, Redis pub/sub publisher, progress coalescing, the WS monitor gateway, and the teacher live-monitor dashboard (LiveMonitorPanel) are all DONE. The attempt:{id} student-facing WS channel is now fully DONE end to end, client included: realtime.Gateway.handleAttempt (server/internal/realtime/gateway.go) authorizes the attempt's owning student only, subscribes to the same quiz:{quiz_id}:events stream as the monitor channel, relays a matching attempt.kicked envelope then force-closes the socket (docs/06 section 4 step 4), and passes through any quiz.* banner event untouched. quiz.extended/quiz.closed publish from quiz/lifecycle.go's Extend/ForceClose (proven by server/internal/quiz/publishflow_test.go). web/src/player/AttemptPlayer.tsx now opens `/ws/attempts/:id` while playing (reconnecting on drop, mirroring LiveMonitorPanel's pattern): an attempt.kicked push renders a reason-aware "You were removed from this quiz" lockout screen (the REST-fallback 409 ATTEMPT_KICKED path is unchanged as a backstop and now renders the same screen instead of a generic one), and quiz.extended/quiz.closed render a dismissable banner strip (extended also silently re-fetches the attempt detail so the countdown picks up any pulled-forward deadline_at). Verified live against a running docker-compose stack in a real Chromium: web/e2e/livemonitor.e2e.mjs was updated (its kicked-student assertion now expects the socket-delivered reason screen, arriving well before the old REST-fallback path would have fired) and passes; an ad-hoc extend/close banner run (not checked in) also confirmed both banners render and dismiss correctly. Remaining:
  - the user:{id}:notify channel is now DONE: realtime.Gateway.handleNotify (server/internal/realtime/gateway.go) authorizes only the URL id matching the caller's own id (no owner lookup needed - the channel name is the resource), subscribes to a new per-user user:{id}:notify Redis channel (realtime.Publisher.PublishNotify, separate from any quiz's events channel), and relays every notification verbatim until disconnect. It carries quiz.assigned/quiz.unassigned (server/internal/quiz/events.go): quiz.Service.SetAssignments (lifecycle.go) now diffs the audience before/after the replace and, after commit, notifies exactly the students whose membership changed - proven by server/internal/quiz/publishflow_test.go's TestQuizAssignmentNotificationsE2E (new student notified, no-op re-save silent, a same-size swap notifies both the added and the dropped student) and server/internal/realtime/gateway_test.go's TestNotify* suite. web/src/player/StudentWorkspace.tsx opens `/ws/users/:id/notify` for the whole signed-in session (not just the list view, since a teacher can assign/unassign while the student is mid-attempt on something else), renders a dismissable banner strip (reusing the quiz.extended/closed `.quiz-banner` styling) for each notification, and remounts the assigned list immediately if it is on screen. Verified live against a running docker-compose stack with a real Chromium (ad-hoc script, not checked in): the socket delivered both quiz.assigned (with the quiz title) and quiz.unassigned, and the banner actually rendered in the DOM. This closed half of "Notifications on assignment changes"; the email leg is now DONE too (see below).
  - heartbeat/disconnect tracking is now DONE: realtime.Gateway.handleAttempt (server/internal/realtime/gateway.go) runs a real read loop (readAttemptHeartbeats) instead of CloseRead's write-only drain, treating any inbound frame on the socket as a heartbeat. web/src/player/AttemptPlayer.tsx sends one every 10s (docs/05 section 5's cadence) for as long as the socket is open. pumpAttempt tracks a per-connection heartbeatTimeout (25s, 2.5x the client cadence, a package var so tests can shrink it): a lapse calls the new AttemptDisconnectedFunc, wired in main.go to attempt.Service.LogAttemptDisconnected (appends+publishes attempt.disconnected, mirroring LogSessionInvalidated's pattern exactly); the next heartbeat after that calls AttemptReconnectedFunc -> LogAttemptReconnected (attempt.reconnected). quiz.LiveRoster (server/internal/quiz/live.go) derives the same "disconnected" roster state for a fresh GET /quizzes/:id/live snapshot from each attempt's most recent attempt.disconnected/attempt.reconnected row - docs/05 section 4's "materialized from attempts plus recent attempt_events" - so a late-joining dashboard shows a lapsed heartbeat too, not just a live delta; the LiveRosterRow.state enum in api/openapi.yaml now includes "disconnected". web/src/authoring/LiveMonitorPanel.tsx applies attempt.disconnected/reconnected deltas, adds a "Disconnected" amber chip, and keeps the countdown/Kick control active on a disconnected row (the deadline clock keeps running per docs/05 section 5). Proven by server/internal/realtime/gateway_heartbeat_test.go (timeout fires disconnected exactly once, regular heartbeats never trip it, a heartbeat after a disconnect fires reconnected, a heartbeat frame is never echoed back to the client) and three new subtests in server/internal/attempt/liveflow_test.go (a disconnect/reconnect flips and restores the roster row live, a stale disconnect logged against an already-submitted attempt never resurfaces it). Verified live against a running docker-compose stack with a real Chromium (ad-hoc script, not checked in): captured the real client actually sending a heartbeat frame over the live socket, and confirmed the teacher's roster flips to "Disconnected" then back to "In progress" over the existing monitor socket with no reload. Full Go test suite, go vet, gofmt, npm typecheck/lint/build, and both existing player.e2e.mjs/livemonitor.e2e.mjs suites all pass with no regressions.
  - current_question wiring is now DONE: attempts.current_question (new migration 00007) stores the 1-based ordinal of the last question SaveAnswer resolved (the closest proxy REST autosave has to a navigation cursor); it rides the existing attempt.progress event/publish and the GET /quizzes/:id/live roster row, and LiveMonitorPanel's progress cell now shows "Q{n} · answered/total". Proven by server/internal/attempt/{eventflow,publishflow,liveflow}_test.go (updated from asserting explicit null to asserting the ordinal).
  - Archive does not publish a quiz.* event, but this is not a gap - docs/05 section 2's vocabulary only lists quiz.extended/quiz.closed, not an archive banner

- Milestone 6 - Guardrails and kick (FR-10, FR-11): the backend is DONE - kick, readmit, violation reporting with count/warn/off policies, the auto_submit violation ladder, and per-teacher rate limiting on kick/readmit (server/internal/ratelimit, shared with login), all proven by E2E suites. The client side is also now DONE: web/src/player/AttemptPlayer.tsx implements fullscreen lock, focus/blur tracking, clipboard/context-menu blocking, and reports to POST /attempts/:id/events. The student lockout screen showing the kick reason and the quiz.extended/close/kick banners - this section's last remaining item - are now DONE too; see Milestone 5.

- Single active session per attempt (docs/08 section 1, docs/06 section 3): now DONE. server/internal/realtime/gateway.go tracks the one open attempt:{id} socket per attempt ID; a second device's connection force-closes the first with WebSocket close code 4001 (statusSessionReplaced) and calls the new SessionInvalidatedFunc callback, wired in main.go to attempt.Service.LogSessionInvalidated - a new attempt_events row (attempt.session_invalidated) is appended and published on the quiz channel, satisfying "logged as an event the teacher can see" (docs/08 section 1). web/src/player/AttemptPlayer.tsx's attempt socket recognizes close code 4001 and shows a "This attempt was opened in another window or device" banner instead of reconnecting (reconnecting would just re-invalidate the newer session back, an endless loop). Verified with gateway unit tests (server/internal/realtime/gateway_session_test.go), a service-level itest (server/internal/attempt/sessioninvalidateflow_test.go), and an ad-hoc two-browser-context Puppeteer run against the live docker-compose stack (not checked in) confirming the stale tab shows the banner while the newer tab keeps working normally.

- Milestone 7 - Bulk import (FR-3): DONE, including the production object store. server/internal/quiz/{importvalidate,importworker,importstorage}.go implement the full pipeline - template parse and validation, upload registration, async validate worker job, review UI with poll/error reporting, and a transactional commit. The object storage gap is now closed: server/internal/quiz/importstorage_r2.go adds R2ImportStorage, an ImportFileStore backed by a Cloudflare R2 bucket via the S3-compatible aws-sdk-go-v2/service/s3 client (path-style addressing, region "auto", per Cloudflare's documented SDK setup) - Save buffers the (already ≤10 MB, MaxImportFileBytes-capped) upload and PutObjects it under a random key, Open GetObjects it back, with the same bare-filename validation LocalImportStorage already applied against path traversal. quiz.NewImportFileStore(dir, r2Bucket, r2Endpoint, r2AccessKeyID, r2SecretAccessKey) (server/internal/quiz/importstorage.go) picks R2ImportStorage when MACQUIZ_IMPORT_R2_BUCKET is set, else falls back to LocalImportStorage against MACQUIZ_IMPORT_DIR - never a boot failure, matching this codebase's "unconfigured optional backend degrades gracefully" contract (Redis, email) - and is wired identically in both server/cmd/macquiz/main.go's serve() and server/internal/worker/worker.go's Run() so both processes read/write the same backend without a shared volume once R2 is configured. New env vars documented in .env.production.example: MACQUIZ_IMPORT_R2_BUCKET/_ENDPOINT/_ACCESS_KEY_ID/_SECRET_ACCESS_KEY. Proven by server/internal/quiz/importstorage_r2_test.go (Save/Open round trip, path-traversal rejection, missing-key error) against a fake s3API double - no real R2 credentials needed to test the logic; the full existing bulk-import E2E suite (importregister/importget/importcommit/importworkerflow_test.go) still passes unchanged since it exercises the interface, not a concrete backend. Full Go test suite passes with no regressions.

- Milestone 8 - Analytics (FR-8, FR-9): DONE, including the previously-missing "score and percentile per quiz" per-student metric (docs/07 section 3 line 37). server/internal/analytics/{rollup,service,http}.go implement the rollup-on-close job (quiz_stats/student_stats: distribution, item analysis, integrity signals), the quiz/student/teacher/org analytics endpoints, CSV result export, the teacher QuizStatsPanel, and the admin OrgStatsPanel. Percentile is now served by GET /attempts/:id/result (Result.percentile, server/internal/attempt/results.go's quizPercentile): a percentile-rank derived from the quiz's already-computed quiz_stats.distribution 10-bucket histogram (below-bucket count plus half the attempt's own bucket, over the total) - no extra full-population query, matching docs/07 section 4's "no separate analytics store" discipline - null until quiz_stats lands or when the quiz has no points. web/src/player/ResultReview.tsx renders it as "{n}th percentile" in the score banner. Proven by a new assertion in server/internal/attempt/releaseflow_test.go (workerPass now also runs analytics.RollupDue, matching the real worker's sweepDue order; alpha's 3/3 in a two-student quiz asserts percentile=75) and live-verified against a running docker-compose stack (GET .../result returned percentile:50 for a lone attempt, and the review page rendered "50th percentile" in the DOM, screenshot-checked). One remaining sub-metric from that same table cell, "strength/weakness by topic tag", is intentionally NOT implemented and is out of v1 scope, not merely deferred: the schema carries no per-question topic taxonomy anywhere (student_stats.topic_strengths is always an empty {} - see rollup.go's own comment) and building it would require a new data model (a topic-tag column/table plus authoring UI to assign tags), which is a larger, separate feature.
  The score-override gap is now DONE: docs/06-attempt-lifecycle.md line 80's "results are flagged kicked wherever scores appear, and the teacher can override the score to zero per attempt" has a real endpoint and UI. POST /api/v1/attempts/{id}/override-score (server/internal/attempt/{service,http}.go's OverrideScore/handleOverrideScore, mirroring kick/readmit's owner-or-admin authorization, required reason, and per-teacher rate limiting) zeroes attempts.score and stamps new score_overridden_at/score_overridden_by columns (migration 00008_attempt_score_override.sql) in one transaction with an attempt.score_overridden audit_log row. The target must be kicked (else 409 ATTEMPT_NOT_KICKED) and already graded (else 409 ATTEMPT_NOT_GRADED - grading's own unconditional score UPDATE would otherwise race and clobber an override applied before it lands); it is idempotent per attempt via the same WHERE-guard pattern kick/readmit use. The overridden flag now surfaces wherever a score appears, per the doc's own phrasing: quiz.ResultRow/attempt.Result both gained a score_overridden bool (GET /quizzes/:id/results, GET /quizzes/:id/results.csv, GET /attempts/:id/result), and web/src/authoring/QuizStatsPanel.tsx (rendered for closed/archived quizzes, the natural point since grading typically lands well before a quiz's window actually closes) now shows a "Kicked attempts" table listing each kicked student's score with an "Override to zero" button that flips to "Score overridden to 0" once used. Proven by server/internal/attempt/overridescoreflow_test.go (authorization mirrors kick/readmit exactly - 403/404/422/409 cases -, the not-graded-yet 409, the zeroing plus audit row, idempotency, an admin override, and the results-table flag) and live-verified end to end against a running docker-compose stack with a real Chromium and the actual UI: kicked a student, confirmed the pre-grade 409, watched the worker grade it, clicked "Override to zero" through the real prompt() dialog, and confirmed both the score and the "Score overridden to 0" label updated in the DOM (screenshots captured). Full Go test suite, go vet, gofmt, npm typecheck/lint/build all pass with no regressions; a genuine header mismatch this surfaced in the existing resultscsvflow_test.go (the CSV gained a trailing score_overridden column) was fixed in the same change.

- Milestone 9 - Production hardening: mostly DONE - docker-compose.prod.yml, Caddyfile, .env.production.example, the nightly pg_dump-to-R2 backup cron plus the exam-day pre-window trigger, OpenTelemetry metrics export from both processes (server/internal/telemetry), the /healthz DB+Redis+queue-lag check, /deploy-check, migrate-on-boot, and a deploy.yml GitHub Action gated on live-quiz status. Worker-process telemetry export is now DONE: worker.Run (server/internal/worker/worker.go) calls telemetry.Setup(ctx, cfg, "macquiz-worker") and registers the same macquiz.queue.lag_seconds gauge serve() does (querying its own DB handle, so lag exports even if the API process is down), plus a new macquiz.worker.due_transitions counter labeled by kind (quizzes_opened, quizzes_closed, attempts_auto_submitted, attempts_force_submitted, attempts_graded, results_released, quizzes_rolled_up) recorded once per sweepDue pass with any nonzero transitions - mirroring the fields already in its "due transitions applied" log line. Verified with a telemetry unit test assertion and the existing DB-backed TestWorkerOpensAndClosesQuiz, which now exercises Setup/RegisterQueueLagGauge/RecordDueTransitions end to end against a real river/Postgres run. The Grafana Cloud dashboard/alert-rule gap is now DONE: scripts/grafana/dashboard.json is a standard Grafana dashboard JSON model with a panel for each docs/10 section 2 key series (autosave p95, WS connection count, queue lag, violation/kick rates) plus the worker's due_transitions counter, querying PromQL against the metric names Grafana Cloud's OTLP ingestion derives from server/internal/telemetry's instruments (e.g. macquiz.attempt.autosave.duration -> macquiz_attempt_autosave_duration_seconds_bucket, the standard OTel-to-Prometheus naming convention). scripts/grafana/alert-rules.json is a Grafana unified-alerting provisioning file covering the two docs/10 section 3 thresholds backed by an app-emitted metric (autosave p95 warn>200ms/page>300ms sustained 5m, queue lag warn>10s/page>60s). Because Grafana Cloud's free tier is a hosted service with no filesystem provisioning, both are meant to be imported once by hand via the UI (or the alert-provisioning HTTP API) - the same "run by hand" convention restore-drill.sh already uses - documented in scripts/grafana/README.md, which also explains why disk-usage and backup-job alerting are deliberately not included (no app-emitted metric backs them; they're a host-agent/dead-man's-switch concern, separate infrastructure work). Both JSON files are syntax-verified but not live-imported against a real Grafana Cloud stack, since none is provisioned in this environment.
  The go-live-herd load test gap is now DONE: `macquiz loadtest-seed` (server/cmd/macquiz/loadtest.go, a new one-shot mode alongside migrate/bootstrap) idempotently provisions a teacher, 2,000 students, and a published quiz assigned to all of them by calling the real quiz/authusers service layer directly (not hand-rolled SQL, so publish's snapshot/version invariants stay honest) - reruns top up or no-op depending on whether the quiz has already gone live, since a live quiz's audience/window are frozen by design. scripts/loadtest/herd.js (k6) then runs two concurrent scenarios matching docs/01's "2,000 concurrent students... 1,000 attempt-starts within one minute" sizing: `steady` (1,000 students already mid-attempt when the run starts, holding their attempt:{id} socket open for the whole run) and `herd` (1,000 fresh logins + attempt starts arriving at a constant rate over 60s, the actual go-live spike) - both heartbeat every 10s and autosave on a timer, matching the real player's cadence, with thresholds on attempt_start_ok, autosave_ms (the docs' literal p95<300ms requirement), ws_connect_ok, and http_req_failed. scripts/loadtest/run.sh orchestrates both steps against the local Compose stack (seeds, then runs k6 in a container sharing the app container's network namespace, so it needs no manual network/URL wiring) and forwards any extra args to `k6 run` for tuning (`-e HERD_STUDENTS=500`, `--out json=...`, etc.).
  Building and exercising this load test caught a real go-live risk it exists to catch: server/internal/db/db.go's Open never bounded the pgx connection pool (database/sql's default is unlimited), so a concurrent request spike opened roughly one Postgres connection per in-flight request - live-verified at 600 concurrent students, this blew straight through Postgres's own max_connections=100 and failed a large fraction of logins/starts with "sorry, too many clients already" (Postgres logs) instead of queuing briefly on a small reused pool. Fixed by adding a config.DatabaseMaxConns knob (env MACQUIZ_DATABASE_MAX_CONNS, default 20, applied to both serve() and worker.Run() - the two long-running, internet-facing processes; migrate/bootstrap/loadtest-seed/itest stay unbounded since they are one-shot and low-concurrency) and calling SetMaxOpenConns/SetMaxIdleConns/SetConnMaxLifetime in db.Open. Re-verified live after the fix: the same 600-concurrent-student run (300 steady + 300 herd) now completes with 0% http_req_failed and 100% of every check passing (login/attempt-start/autosave/ws-upgrade all succeed), and at 300 concurrent students the autosave_ms p95 threshold (<300ms) passes cleanly (187ms, then 187ms/1.8s across two more runs as concurrency rose). At the full 600-concurrent-peak scale, autosave p95 exceeds 300ms on this development sandbox specifically - `docker stats` during a run showed the app container alone pegged near 50% of the box's 4 cores (Postgres stayed under 2%), consistent with Argon2id's deliberately CPU-expensive login hashing (docs/08-security.md's OWASP-recommended parameters) contending with Postgres/Redis/worker/the k6 load generator itself all colocated on one small shared VM - not a functional defect (zero errors at any scale tested). Full Go test suite (go build/vet/test, all DB-backed itests) passes with no regressions. Running this at the full 1,000-herd/2,000-total scale against representative target-sized infrastructure (docs/12: "run before launch and before each exam season"), separate from the load generator, is the intended real verification and remains an operational task, not a code gap - the tooling to do it now exists and is proven correct.
  The restore-drill gap is now DONE: scripts/backup/restore-drill.sh pulls the latest daily/ dump from R2 (or drills a local file via --dump-file, for testing the drill itself without R2 credentials), restores it into a scratch postgres:16-alpine container on an isolated Docker network, runs the app image's own "migrate" command against the restored database as a schema-compatibility check (a real regression test - it fails loudly if a restored dump's schema is missing something the current code expects), and finally queries row counts on the core tables (users, quizzes, quiz_versions, attempts, attempt_events, audit_log) to prove the data is actually queryable, not just present as bytes. set -euo pipefail plus a trap means any failure (bad dump, migrate error, unreachable DB) exits non-zero and still tears down the scratch container/network. Verified live: built the app image from server/Dockerfile, dumped a real seeded dev Postgres database with pg_dump -Fc, ran the drill against it end to end (PASS, correct row counts, migrate reported 0 pending), and separately fed it a corrupt file to confirm it fails loudly and still cleans up. Not wired into any cron - docs/10-operations.md section 1 calls for this to be run by hand once per term.

- Answer autosave rate limiting (docs/08-security.md section 4 line 29, docs/04-api.md section 5 line 122): now DONE. A fresh audit of docs/08/11/03/04 (sections that hadn't been recent iterations' specific focus) found this endpoint was the one rate-limit the docs named that was never wired up - PUT /attempts/:id/answers/:qid had no server-side limiter at all, only a 2s client-side debounce (trivially bypassed by a direct API call). Fixed with server/internal/attempt/http.go's new saveAnswerByAttempt limiter (ratelimit.New(120, time.Minute), keyed by attempt ID per the docs' "per attempt" wording, generous relative to the 2s debounce's ~30/min steady rate), checked in handleSaveAnswer before any body decode/business logic, mirroring the existing kick/readmit/override-score per-teacher limiters exactly (same ratelimit package, same httpapi.WriteRateLimited 429 response). api/openapi.yaml's saveAnswer operation gained the missing 429 RateLimited response (schema.d.ts regenerated). Proven by a new subtest in server/internal/attempt/ratelimitflow_test.go (renamed TestAttemptRateLimits to cover all three attempt-side limiters): 120 autosaves succeed, the 121st inside the same minute answers 429 RATE_LIMITED. The existing client save-error path (web/src/player/AttemptPlayer.tsx's saveOne) already handles any non-2xx/non-409 response generically - it re-marks the question dirty and shows "Could not save your answer. It will retry on your next change" - so a 429 (which should never occur in normal debounced play) degrades exactly like any other transient save failure with no client change needed. Full Go test suite, go vet, gofmt, npm typecheck/lint/build all pass with no regressions.
  That same audit also surfaced three real, previously-unflagged gaps against docs/11-frontend-design-system.md; the Toast gap and the destructive-flow modal gap are now DONE, one remains open:
    - Toast component: now DONE. web/src/toast/{context.ts,Toast.tsx} implements docs/11 section 4's one sanctioned style exactly - ink surface (#131a26/--color-ink), a semantic 8px dot (success=green/--color-success-dot, info=blue/--color-primary, warning=amber/--color-warning-dot, danger=red/--color-danger), fixed top-center, auto-dismiss at 3.2s, floating shadow, respects prefers-reduced-motion via index.css's existing global rule. `<ToastProvider>` wraps the whole app in App.tsx (outside AuthProvider, so it is available on every screen) and exposes `useToast().showToast(message, kind)`; only one toast shows at a time, a newer call replacing whatever is up rather than stacking, matching the doc's "one style" framing and the removed inline mechanism's existing single-message behavior. Wired into the two call sites the doc names by name: AttemptPlayer.tsx's guardrail violation reporting now calls showToast(..., 'warning') instead of rendering an inline .guardrail-notice paragraph (that CSS class and its backing violationNotice/noticeTimer state were deleted, dead code now that the toast owns this), and a manual submit's success path shows showToast('Quiz submitted - graded instantly.', 'success') before the done screen (timeup/kicked/closed terminal reasons do not get this toast - it is specifically manual-submission copy per the doc's exact example string). Live-verified against a running docker-compose stack plus `npm run dev` with a throwaway Puppeteer script: dispatching a clipboard paste event inside the player showed the amber-dot toast with the documented copy, it auto-dismissed after ~3.2s, and a real submit flow showed the green-dot "Quiz submitted - graded instantly." toast (screenshots confirmed ink surface, top-center placement, correct dot colors rgb(247,144,9) amber / green). npm run typecheck/lint/build all pass with no regressions.
    - Destructive-flow modal: now DONE. web/src/components/DestructiveConfirmModal.tsx is a shared, reusable two-step component matching docs/11 section 4 exactly: overlay `rgba(13,18,28,.52)` + `blur(2px)` (index.css's new .modal-overlay), 16px-radius floating-shadow panel, step 1 collects a required reason (no red button yet - Cancel/Continue), step 2 restates the consequence in a danger-tint card (.modal-consequence, reusing --color-danger-tint/--color-danger-on-tint) with Back plus the red confirm button (.button-danger) - the doc's literal kick-flow copy ("Confirm removal / student · quiz / Reason / [Back] [Remove student]") is reproduced verbatim as the kick call site's title/subtitle/confirm label. Wired into all three call sites the prior audit named, replacing window.prompt() at each: LiveMonitorPanel.tsx's escalate() (kick and readmit - QuizEditor.tsx now passes quizTitle down for the "student · quiz" subtitle), and QuizStatsPanel.tsx's overrideScore() (also newly receiving quizTitle). Live-verified against a running docker-compose stack plus `npm run dev`: updated the checked-in web/e2e/livemonitor.e2e.mjs (it previously drove the kick flow via a JS `dialog` handler for window.prompt, which no longer fires) to drive the real two-step modal instead - full suite still passes, including new assertions that the modal opens, the confirm step's danger-tint card reads "ends their attempt immediately", and the roster still flips to Kicked after confirming. A second throwaway Puppeteer script (not checked in) drove a closed quiz's QuizStatsPanel end to end - kicked-attempts table renders, Override to zero opens the modal, confirming flips the row to "Score overridden to 0" - screenshots confirmed the same overlay/card/button styling as the kick flow. npm run typecheck/lint/build all pass with no regressions; no other e2e file referenced window.prompt or the override-score UI.
    - No leaderboard / "dark island" component (docs/11 section 4, section 6): a slate-ramp ranked list with gold/silver/bronze medallions. Scope call made this iteration, after three iterations of leaving it open: this is intentionally NOT implemented, and is out of v1 scope, not merely deferred - same treatment as Milestone 8's topic-tag sub-metric. Reasoning: (1) "leaderboard"/rank/standings appears nowhere in docs/01-10's functional requirements - grepped for rank|leaderboard|standing across every FR doc and found nothing beyond the already-implemented bucket-granular percentile (Milestone 8); (2) no API endpoint anywhere exposes one student's name/score to another student to feed a leaderboard - GET /quizzes/:id/results (docs/04 line 57) is explicitly teacher/owner-only, and grepping server/internal/attempt and server/internal/quiz for any cross-student read path found none; (3) building one would mean inventing a new feature (a new endpoint broadcasting other students' identities/scores) unsanctioned by any requirement, and in tension with docs/01 line 36's "no public/anonymous access" privacy stance. docs/11 section 4/6's leaderboard entries read as carried-over design-mockup surface area (`design_system.html`) rather than a translated product requirement. If ever wanted, it needs a real product decision (anonymized vs named ranks, opt-in/out, cohort scope) before any implementation, not just a UI build against the existing percentile data.

Smaller design-doc items with no home yet

- Admin console UI: DONE - web/src/admin/{AdminWorkspace,UsersPanel,GroupsPanel,OrgStatsPanel}.tsx, no longer a placeholder.
- Audit log read side: DONE - GET /api/v1/audit, keyset-paginated, admin-only.
- Live-quiz teacher controls (extend, force-close, archive): DONE, including all the read-path widening for the archived terminal state, and now also the quiz.extended/quiz.closed realtime publish and the client rendering it as a banner (see Milestone 5). No known remaining gap here.
- Notifications on assignment changes (in-app/email): now fully DONE, both legs. The in-app half - see the user:{id}:notify entry under Milestone 5 above (quiz.assigned/quiz.unassigned over the new channel, rendered as a banner in web/src/player/StudentWorkspace.tsx). The email leg: quiz.EmailSender (server/internal/quiz/email.go) is a small Send(ctx, to, toName, subject, textBody) interface, decoupled from any provider the same way EventPublisher/SnapshotCache are (quiz never imports net/http); quiz.Service.emailAssignmentChanges (lifecycle.go), called from SetAssignments right after the existing PublishNotify loop, sends one email per student whose audience membership actually changed - an "Assigned: {title}" mail for a newly-added student (reusing assignedStudents' post-commit query for name/email, no extra round trip), a "Removed: {title}" mail for a dropped one (a small usersByID lookup, since a dropped student is no longer in that query's result). Each send runs in its own goroutine on a context.WithoutCancel + 10s-bounded timeout, so a slow or unreachable provider can never add external-API latency to the PUT /quizzes/:id/assignments request - the same "publish never blocks the write path" discipline docs/05 section 1 established for Redis, extended to a slower transport. email.ResendSender (new package server/internal/email) is the concrete implementation: a thin client for Resend's POST /emails API (docs/09-deployment.md section 3's other documented option besides Brevo), wired in main.go only when MACQUIZ_EMAIL_API_KEY is set (config.go: MACQUIZ_EMAIL_API_KEY/MACQUIZ_EMAIL_FROM/MACQUIZ_EMAIL_FROM_NAME, documented in .env.production.example); leaving it unset degrades to quiz's noopEmailSender default, never a boot failure, since the in-app channel already delivers the same event regardless. Proven by server/internal/quiz/emailflow_test.go's TestQuizAssignmentEmailsE2E (new student emailed, no-op re-save silent, a same-size swap emails both the added and the dropped student - mirroring TestQuizAssignmentNotificationsE2E's cases for the WS leg) and server/internal/email/email_test.go (the Resend request shape: Authorization header, from/to formatting, and a non-2xx response surfacing as an error). Full Go test suite passes with no regressions.
- Single active session per attempt: now DONE - see the new entry under Milestone 6 above.
- CI running golangci-lint (docs/12-implementation-plan.md Milestone 0 line 18): now DONE. A fresh audit of docs/02/09/12 (which hadn't been a recent iteration's specific focus) found the implementation plan explicitly named golangci-lint as part of Milestone 0's CI, but .github/workflows/ci.yml only ever ran gofmt/go vet/go test/go build - the named linter was never adopted. Fixed: .github/workflows/ci.yml's server job now runs golangci-lint (golangci-lint-action@v7, pinned to v2.12.2 to match the local verification) alongside the existing checks; server/.golangci.yml uses the default linter set (errcheck, govet, ineffassign, staticcheck, unused) with an errcheck exclude-functions list for defer'd best-effort resource cleanup (DB/rows/file/socket/pub-sub Close, telemetry Shutdown) - each entry documented as intentional, not a blanket "ignore all Close errors" suppression. Running it surfaced two real, if minor, findings that are now fixed: two staticcheck S1016 hints (authusers/adminhttp.go, quiz/http.go - both request-to-patch struct conversions now use a direct type conversion instead of a field-by-field literal) and one genuine security bug it was never designed to specifically look for but caught anyway via a staticcheck SA1019 deprecation notice - see the next entry.
- Login rate limit was bypassable via a spoofed IP header (docs/08-security.md section 4's "login rate limits per IP"): now DONE/fixed. server/internal/httpserver/server.go used chi's deprecated middleware.RealIP, which trusts the leftmost X-Forwarded-For value - entirely client-controlled, so anyone could bypass the per-IP login limiter (h.loginByIP.Allow(r.RemoteAddr, ...)) just by sending a different fake header on every request (chi's own deprecation notice cites GHSA-3fxj-6jh8-hvhx for exactly this). Replaced with a small clientIP middleware matching this app's actual two deployment shapes (docs/09 section 4): trusts CF-Connecting-IP (Cloudflare overwrites this on every request it proxies - a client cannot forge it once traffic reaches Cloudflare's edge) and falls back to the bare TCP peer address for local/dev, where docker-compose hits the app directly with no proxy in front; X-Forwarded-For is never consulted. This also fixes a latent, lower-severity issue for free: previously in dev (no proxy, so RealIP left RemoteAddr as chi's untouched "ip:port"), every request got a different rate-limit key since the port varies per TCP connection - the new middleware always strips the port, so the key is stable everywhere. Proven by a new TestClientIP (server/internal/httpserver/server_test.go): CF-Connecting-IP is trusted, a header-less request falls back to the stripped TCP peer, and - the regression test for the actual vulnerability - a spoofed X-Forwarded-For is confirmed to have zero effect. Full Go test suite, go vet, gofmt, go build, and golangci-lint (0 issues) all pass.
- No leaderboard / "dark island" component (docs/11 section 4/6): scope call made and closed out this iteration - see the updated entry under the docs/11 audit below. Intentionally NOT implemented, out of v1 scope.
- CI never actually provisioned Redis (docs/09-deployment.md section 5's "Postgres/Redis as Actions services"): now DONE/fixed. A fresh audit of docs/02/07/09/10/12 (sections that hadn't been a recent iteration's specific focus) found .github/workflows/ci.yml only ever defined a postgres service container - its own header comment ("Redis service container is added when the milestone that needs it lands") was stale, since Milestone 5's realtime/Redis pub/sub had long since landed. Three real integration tests (server/internal/realtime/{publisher_test.go,snapshotcache_test.go,gateway_live_test.go}) gate on a reachable Redis at redis://localhost:6380/0 (or MACQUIZ_TEST_REDIS_URL) and t.Skipf otherwise - since CI never exposed that port, all three silently skipped on every CI run, so the Redis pub/sub publisher, snapshot cache, and live WS gateway paths were never actually exercised there despite the doc's explicit claim. Fixed by adding a redis:7-alpine service (port 6380->6379, redis-cli ping healthcheck, matching docker-compose.yml's own dev port mapping) plus MACQUIZ_TEST_REDIS_URL to the server job. Live-verified locally: with Redis down, the three tests SKIP with the exact "redis not reachable" message; with the new service's port mapping running, all three PASS. Full Go test suite (build/vet/gofmt/golangci-lint/test) passes with Redis up, and the previously-skipped tests are now confirmed to actually execute.
- oapi-codegen doc/code mismatch (docs/02-architecture.md line 100, docs/12-implementation-plan.md line 15): FURTHER progress this iteration on the "migrate handlers package-by-package" follow-up a prior iteration scoped and deliberately left open. The analytics package - the one explicitly named as the natural starting point (smallest, read-only, lowest risk) - is now migrated: analytics.QuizStats (server/internal/analytics/service.go) is no longer a hand-written struct but a direct type alias to the generated apischema.QuizStats, and Service.QuizStats builds it for real - decoding the quiz_stats jsonb columns (distribution, item_analysis, integrity) into the generated typed fields ([]int, []apischema.ItemStat, apischema.IntegritySummary) instead of passing raw bytes through, parsing the quiz id into the generated openapi_types.UUID field, and narrowing mean/median/participation from Postgres's float64 to the generated type's *float32 via a documented float32ptr helper. The float32-precision question flagged by the prior iteration as needing a deliberate decision is now resolved, not deferred: these are display-only percentages/scores (docs/07 section 3), never a financial computation, so float32's ~7 significant digits lose nothing that matters on the wire - the wire format itself is unchanged either way (distribution/item_analysis/integrity were always real embedded JSON on the wire, never a JSON string, since json.RawMessage marshals inline; only the Go-side internal representation changed from bytes-passthrough to typed-decode). This is a real, not just type-level, migration: the DB-scan code in service.go was rewritten, exactly the "touching DB-scan logic, not just the HTTP boundary" work the prior iteration identified as blocking a mechanical migration. Proven by the existing server/internal/analytics/rollupflow_test.go's TestRollupFlowE2E (its "the owner reads the graded quiz's rolled-up stats" subtest asserts mean/median/participation/item_analysis/integrity content over the real HTTP JSON response) - re-run against a live Postgres instance, it caught the float32 rounding for real (participation's literal decimal value shifted from 0.6666666666666666 to 0.6666667f), and the test's tolerance was widened from 1e-9 to 1e-6 to match float32's actual precision rather than silently loosening past what the type can represent. Full Go test suite (go build/vet/test against a live Postgres DB), gofmt, and golangci-lint (0 issues) all pass with no regressions; `go generate ./internal/apischema/...` reports no diff (the generated types were already in sync, untouched by this change) and the web TS client model is unaffected (schema.d.ts's QuizStats shape was already number-typed, driven by the spec, not by the Go struct). Two other packages sharing the same shape of hand-written-struct-vs-generated-type mismatch remain: quiz.ResultRow (score_overridden, CSV export) and attempt.Result (percentile) - StudentStats/TeacherStats/OrgStats/LiveRosterResponse also still use hand-written structs. These are the natural next targets, each needing its own field-by-field comparison against the generated type before migrating (the same investigation this iteration did for QuizStats), not a blanket mechanical rename.
- oapi-codegen migration, second package (docs/02-architecture.md line 100, docs/12-implementation-plan.md line 15): quiz.ResultRow (server/internal/quiz/results.go), the GET /quizzes/:id/results and GET /quizzes/:id/results.csv row shape, is now migrated the same way analytics.QuizStats was: it is a direct type alias to the generated apischema.ResultRow instead of a hand-written struct, and Service.Results builds it for real - student_id/attempt_id now parse into the generated openapi_types.UUID field (uuid.Parse, erroring the request rather than silently zero-valuing on a malformed row), email narrows to openapi_types.Email, status/submit_kind scan through sql.NullString into the generated apischema.ResultRowStatus/ResultRowSubmitKind typed enums instead of raw *string, and score/max_score narrow from Postgres float64 to the generated type's *float32 via a new resultFloat32 helper (same float32-is-correct-for-display-only-scores reasoning as the analytics migration, not re-litigated). resultshttp.go's CSV writer was updated in lockstep: the old generic strPtrString/floatPtrString(*float64) helpers were replaced with resultRowStatusString/resultRowSubmitKindString (typed-enum-aware) and floatPtrString now takes *float32, since the CSV export reuses Service.Results directly and must stay byte-for-byte consistent with the JSON view. Wire format is unchanged (all JSON field names/shapes were already spec-driven, matching what apischema.ResultRow now also encodes), so no api/openapi.yaml or web/src/api/schema.d.ts change was needed - confirmed by grepping the frontend's actual field accesses (QuizStatsPanel.tsx's row.submit_kind/row.score_overridden) which are untouched. Proven by the full existing internal/quiz test suite (including resultscsvflow_test.go's byte-level CSV header/score assertions and archiveflow_test.go's post-archive results read), `go generate ./internal/apischema/...` reporting no diff, and golangci-lint reporting 0 issues; `go mod tidy` confirms no new dependency (google/uuid and oapi-codegen/runtime were already direct deps from the analytics migration). StudentStats/TeacherStats/OrgStats/LiveRosterResponse and attempt.Result remain hand-written and are the next targets.
- oapi-codegen migration, third/fourth/fifth packages (docs/02-architecture.md line 100, docs/12-implementation-plan.md line 15): StudentStats, TeacherStats, and OrgStats (all server/internal/analytics/service.go, the three remaining analytics response types the prior iteration named as next targets) are now direct type aliases to the generated apischema.StudentStats/TeacherStats/OrgStats instead of hand-written structs, and Service.StudentStats/TeacherStats/OrgStats build them for real. StudentStats: student_id parses into the generated openapi_types.UUID (uuid.Parse, 404 on a malformed id, matching the existing existence-safe-404 contract); avg_time_per_question/completion_rate narrow to *float32 via the existing float32ptr helper; accuracy_trend and topic_strengths - previously raw json.RawMessage passthrough - now json.Unmarshal straight into the generated typed fields ([]struct{Accuracy *float32; QuizId openapi_types.UUID; SubmittedAt time.Time} and map[string]interface{}), which works with zero rollup.go changes since rollupStudents' jsonb_build_object output (quiz_id/accuracy/submitted_at, plus the '{}'::jsonb topic_strengths) was already structurally identical to the generated shape. TeacherStats: teacher_id parses the same way; avg_participation/avg_class_score/avg_publish_to_results_sec all narrow to *float32 via float32ptr. OrgStats: active_users scans directly into the generated type's anonymous ActiveUsers struct (int fields, no change needed); quizzes_per_week's loop variable is now a local anonymous struct with the exact field names/tags/order of the generated type's anonymous QuizzesPerWeek element type (Go's struct-identity assignability rule requires this, not just matching field names) so it appends into the slice with no separate named type; cohort_comparisons is now `type CohortStats = apischema.CohortStats`, with group_id parsed via uuid.Parse (previously a bare string) and avg_completion_rate/avg_accuracy narrowed via float32ptr. Every helper/handler-boundary code (analytics/http.go) needed zero changes since it only round-trips these types through Service calls to JSON encoding. Proven by the full existing rollupflow_test.go suite against a live Postgres instance (student/teacher/org dashboard subtests all assert real HTTP JSON response content, not just status codes) - three assertions (avg_participation, cohort avg_completion_rate, cohort avg_accuracy) compare a non-power-of-two-fraction value and needed their tolerance widened from 1e-9 to 1e-6 to match float32's actual precision, the same real (not cosmetic) tolerance fix the QuizStats migration required; full Go test suite, go vet, gofmt, golangci-lint (0 issues), `go generate ./internal/apischema/...` (no diff), and `go mod tidy` (no dependency change) all pass with no regressions. This closes the entire oapi-codegen migration list the prior three iterations built up: analytics.QuizStats, quiz.ResultRow, and now StudentStats/TeacherStats/OrgStats are all migrated. Two items remain, deliberately not attempted this iteration: attempt.Result (percentile) is a materially larger, riskier migration than any package done so far - unlike ResultRow it embeds the generated Attempt type (id/quiz_id become openapi_types.UUID, status/submit_kind become generated enums) which is referenced pervasively across every attempt-package response (Start/Get/Submit/list), not just one read-only endpoint, and ResultQuestion's generated shape (typed QuestionBody/QuestionOption/interface{} fields vs. the hand-written version's json.RawMessage passthrough) would require parsing the polymorphic per-question-type snapshot body, not just narrowing numeric types - both are real, scoped-for-their-own-iteration migrations, not a drive-by fix. LiveRosterResponse (server/internal/realtime, embeds Quiz and LiveRosterRow) was not investigated this iteration and its scope/risk is unknown - the next iteration should start there with the same field-by-field comparison this iteration and the prior two did before touching it.
- oapi-codegen migration, sixth package (docs/02-architecture.md line 100, docs/12-implementation-plan.md line 15): investigated LiveRosterResponse and found it lives in server/internal/quiz/live.go (GET /quizzes/:id/live), not server/internal/realtime as the prior iteration's note guessed - the realtime package only publishes deltas over the roster the quiz package materializes. quiz.LiveRow is now `type LiveRow = apischema.LiveRosterRow` instead of a hand-written struct, following the same pattern as ResultRow: student_id/attempt_id parse into openapi_types.UUID (uuid.Parse, erroring the request on a malformed row), email narrows to openapi_types.Email, status/submit_kind scan through sql.NullString into the generated apischema.LiveRosterRowStatus/SubmitKind typed enums, score/max_score narrow from Postgres float64 to *float32 via a new liveFloat32 helper (same display-only-score reasoning as every prior migration in this series), and rosterState() - which derives the dashboard's collapsed state - now takes sql.NullString and returns the generated apischema.LiveRosterRowState enum instead of a bare string. The HTTP handler (livehttp.go) was deliberately left untouched: it composes the response as `map[string]any{"quiz": q, "roster": roster, "server_time": serverTime}` because quiz.Quiz itself remains hand-written (migrating it is the same categorically-larger, pervasively-embedded problem attempt.Result has, not in scope here) - only the LiveRosterRow half of LiveRosterResponse was migrated, matching how a partially-generated response was already the accepted shape for this migration series. Wire format is unchanged (all JSON field names/shapes were already spec-driven); confirmed by the two existing live-roster-specific test suites, both decoding the JSON generically via map[string]any so no test needed float-tolerance changes (the one asserted score value, 3, is exactly representable in float32): TestLiveRosterE2E (server/internal/attempt/liveflow_test.go, the dedicated roster suite) and the live-roster subtest inside TestKickFlowE2E (kickflow_test.go), both re-run against a live Postgres instance and passing. Full Go test suite (go build/vet/test against live Postgres, including realtime/attempt/quiz packages), gofmt, golangci-lint (0 issues), and `go mod tidy` (no dependency change - uuid/oapi-codegen/runtime were already direct deps) all pass with no regressions. This leaves exactly one item in the oapi-codegen migration series: attempt.Result (percentile), previously scoped as materially larger/riskier than every package migrated so far since it embeds the generated Attempt type pervasively across Start/Get/Submit/list and requires parsing ResultQuestion's polymorphic per-question-type body - still deliberately not attempted, still the natural next (and last) target.
- oapi-codegen migration, seventh and final package (docs/02-architecture.md line 100, docs/12-implementation-plan.md line 15): attempt.Attempt and attempt.Result are now DONE, closing the oapi-codegen migration series entirely. The blocker prior iterations flagged - Attempt is embedded pervasively across Start/Get/SubmitManual/Kick/Readmit/OverrideScore/ReportViolation, not just one read-only endpoint - turned out to be mechanical once actually attempted: attempt.Attempt is now `struct { apischema.Attempt; StudentID string \`json:"-"\` }` (composition, not a plain alias, since the spec's Attempt schema deliberately omits the server-internal owner id), and every one of the ~40 internal call sites across service.go was updated - direct SQL query args keep working unchanged (uuid.UUID implements database/sql's Scanner/Valuer), status/submit-kind string-literal comparisons (`a.Status == "kicked"`) keep working unchanged (Go allows comparing a named string type to an untyped string constant), and only the calls crossing into string-typed helpers (appendEvent, enqueueGradeJob/enqueueDeadlineJob, s.events.Publish, guardrailPolicy/guardrailLadder, audit.Write, the SnapshotCache) needed an explicit `.String()`. The second half of the prior scoping - "ResultQuestion's polymorphic per-question-type body" - turned out to be a mischaracterization once the actual generated type was read: QuestionBody is a uniform `{text, image_ref}` shape across every question type (not polymorphic at all; only Options/Correct/Response vary by type), and the generated ResultQuestion types Correct/Response as plain `interface{}`, so decoding the stored raw JSON into `any` (a few-line json.Unmarshal, no type switch) was all the "polymorphic" decoding actually required. attempt.ResultQuestion and attempt.Result are now direct aliases (`= apischema.ResultQuestion` / `= apischema.AttemptResult`); a new buildResultQuestion helper does the per-question conversion (question id parses via uuid.Parse, Correct/Response decode into `any`, Options decodes into `*[]apischema.QuestionOption` when present, Type narrows to the generated enum) and a resultFloat32 helper narrows score/points/percentile from Postgres float64 to the wire type's float32 (same display-only-score reasoning as every prior migration in this series - confirmed no precision loss matters here since the E2E test's asserted values, score 3/max_score 3/percentile 75, are all exactly representable). Wire format is unchanged; confirmed by the full existing internal/attempt test suite (TestReleaseFlowE2E's dedicated "released review carries score, key, and per-question grading" subtest plus every other attempt flow test - player/kick/readmit/override-score/violation/deadline/grade/live), all decoding responses generically via map[string]any, re-run against a live Postgres instance and passing with zero test changes needed. Full Go test suite (go build/vet/test against live Postgres, all packages), gofmt, golangci-lint (0 issues), and `go mod tidy` (no dependency change - uuid/apischema were already direct deps) all pass with no regressions. Every one of the seven originally-named oapi-codegen migration targets (analytics.QuizStats/StudentStats/TeacherStats/OrgStats, quiz.ResultRow, quiz.LiveRow, attempt.Attempt/Result) is now DONE - the migration series that started as "oapi-codegen generates types but nothing builds responses from them" is fully closed.
- Stale admin-console E2E assertions (web/e2e/auth.e2e.mjs, web/e2e/admin.e2e.mjs): now DONE/fixed. Neither of these files, nor any prior audit, had actually been re-run against the current build since AdminWorkspace.tsx grew a new default-landing "Overview" tab (the OrgStatsPanel dashboard added during the Milestone 8 analytics work) in front of the original Users/Groups nav order - a regression no iteration's "all tests pass" claim had caught because both suites require a live docker-compose stack plus a real Chromium, so they only ran ad hoc, not in the Go/npm suites the standard iteration verification loop actually executes. Running them for real this iteration found both broken: auth.e2e.mjs's two admin-landing checks silently failed (asserting `.page-title` reads "Users" immediately after sign-in, when it now reads "Overview"), and admin.e2e.mjs crashed outright - its "land on Groups" step clicked `.rail-nav .rail-item:nth-child(2)`, which was Groups only when Users was position 1; with Overview now first, nth-child(2) is Users, so the click landed on the wrong panel and the next line's `.page-head .button-primary` selector didn't exist there. Fixed by asserting "Overview" as the real post-sign-in landing title in both files, adding a `clickButtonWithText(page, text, scope)` helper (already an established pattern in authoring/analytics/import/player e2e files) to admin.e2e.mjs, and switching its two nav clicks from position-based `nth-child(2)` to text-based lookups ("Users", "Groups") so a future nav reorder can't silently break them the same way again. Verified live: rebuilt and ran the full docker-compose stack fresh, both files now pass every check (auth.e2e.mjs 11/11, admin.e2e.mjs 12/12), and six other previously-unverified-this-run e2e suites (authoring, import, publish, resultscsv, analytics) were also re-run against the same live stack and confirmed still green with no other staleness found. A drive-by fix in the same pass: web/e2e/analytics.e2e.mjs's dashboardFlow took an unused `quizId` parameter (oxlint's only outstanding warning in the repo) - removed, along with the now-unused destructured binding at its call site. This is a caution for future iterations: a green Go/npm CI-equivalent run does not mean the browser E2E suites are green, since none of them execute except when an iteration deliberately spins up docker compose and runs them by hand - that gap in the standard verification loop is exactly how this regression went undetected across however many milestones separate "Overview" being added from now.
- Live-quiz audience "late invite" (quiz-system-design.html section 8, the original SDD - not previously translated into docs/06-attempt-lifecycle.md or tracked anywhere): now DONE. The SDD states plainly: "Adding students while a quiz is live is allowed (late invite); removing a student who has an in-progress attempt from the assignment is blocked." The actual implementation did the opposite - quiz.Service.SetAssignments (lifecycle.go) unconditionally refused any audience edit once a quiz left draft/scheduled (ErrNotEditable/409 QUIZ_NOT_EDITABLE), and docs/06 line 20 echoed the blanket freeze uncritically ("Once Live: the teacher can only extend ends_at, force-close early, or kick individual students"). Found by an Explore-agent audit specifically scoped to the three largish HTML design docs at the repo root (quiz-system-design.html, deployment-plan.html, high-level-architecture.html) that no prior iteration had cross-checked against the implementation - a blind spot distinct from the docs/*.md files every earlier audit targeted. Fixed: SetAssignments now stays open through "live" (using effectiveStatus, so a stored-scheduled quiz whose starts_at has lazily passed is treated as live too, matching every other lifecycle method's convention) - adding a student is unconditional (a late invite), but a replacement that would drop a student with an in-progress attempt is refused whole (the existing "one bad id changes nothing" atomicity), returning a new 409 ASSIGNMENT_IN_PROGRESS (httpapi.CodeAssignmentInProgress, service.ErrAssignmentInProgress) - kick remains the only sanctioned way to interrupt a live attempt, per the SDD's own next sentence. The removed-candidate assignment rows are locked FOR UPDATE before the in-progress count check, mirroring attempt.Service.Start's own FOR UPDATE lock on the same table, so a concurrent Start cannot create an in-progress attempt in the race window between the check and the DELETE. docs/06 line 20 and api/openapi.yaml's PUT /quizzes/:id/assignments description were corrected to state the real rule; a new AssignmentConflict response (409, switches on QUIZ_NOT_EDITABLE vs ASSIGNMENT_IN_PROGRESS) replaces the old single-code NotEditable ref for that endpoint, and web/src/api/schema.d.ts / server/internal/apischema were regenerated. Client: the audience picker (previously only inline in PublishPanel, draft/scheduled-only) was extracted into a shared web/src/authoring/AudienceEditor.tsx so LiveMonitorPanel could reuse it - PublishPanel is unchanged in behavior (still renders AudienceEditor + ScheduleSection); LiveMonitorPanel gained a "Manage audience" toggle that reveals the same editor in a `live` mode (a live-specific hint explains late-invite/kick-instead-of-remove), and any 409 from a blocked removal surfaces through the picker's existing generic save-error path with no new error-handling code needed. Proven by server/internal/attempt/assignmentflow_test.go's TestLiveAssignmentFlowE2E (late invite succeeds live, removing a never-started student succeeds, removing an in-progress student is refused with the audience left untouched, and removing that same student succeeds once kicked) and an updated server/internal/quiz/lifecycleflow_test.go subtest (the old "live quiz audience is frozen" assertion was itself testing the bug - rewritten to assert the correct late-invite-succeeds/publish-still-refused behavior). Live-verified end to end against a running docker-compose stack with a real Chromium and the actual UI (screenshot captured): a teacher opened a live quiz, expanded "Manage audience," checked a new student, saved, and the audience count updated to reflect the late invite live on screen; the existing publish.e2e.mjs and livemonitor.e2e.mjs suites (which exercise the now-shared AudienceEditor component from both call sites) both still pass with no regressions. Full Go test suite, go vet, gofmt, golangci-lint (0 issues), npm typecheck/lint/build all pass.

49. DONE - Bulk-import XLSX support (docs/07-authoring-imports-analytics.md section 2 step 1: "teacher downloads a CSV/XLSX template"): the template doc has named XLSX as a supported upload format since Milestone 7, but the parser (quiz.ParseImportCSV) only ever understood CSV, and the web ImportPanel's file picker and label were hardcoded to CSV alone - the "/XLSX" half of the documented format was never built. Found by a background Explore-agent audit specifically scoped to docs/09/10 (checked out clean, no gap) and docs/11's newer shared components (found one minor raw-hex-vs-token nit, also fixed - see below) alongside this. Fixed: a new server/internal/quiz/importxlsx.go adds ParseImportXLSX, a dependency-free .xlsx reader built on the standard library's archive/zip and encoding/xml (no new go.mod dependency) that resolves the first worksheet (xl/worksheets/sheet1.xml, falling back to the lexicographically-first xl/worksheets/*.xml entry - full workbook.xml/rels sheet-order resolution was scoped out as unneeded complexity for a single-sheet template upload), reads xl/sharedStrings.xml when present, and handles shared-string, inline-string, numeric, and boolean cell types, placing each cell by its real column ref (not XML position) so spreadsheet software's habit of omitting empty trailing cells can't silently shift later columns. The row-level validation logic itself (unknown type, missing/invalid correct answer, duplicate question text, malformed points, the 500-row cap) was extracted out of ParseImportCSV into a new shared parseImportRecords(header, records) so CSV and XLSX can never validate a row differently. A new ParseImportFile(r io.Reader) sniffs the format from the file's own leading bytes (the "PK\x03\x04" ZIP signature XLSX always has) rather than trusting a client-supplied content type, and server/internal/quiz/importworker.go's ValidateImport now calls it instead of ParseImportCSV directly - the only worker-side change needed. Client: web/src/authoring/ImportPanel.tsx's file input now accepts .xlsx alongside .csv, the panel label/button read "CSV or XLSX", and the upload request's Content-Type is chosen from the file's own extension (still cosmetic - the server sniffs bytes, not this header) instead of being hardcoded to text/csv. Proven by new unit tests (importxlsx_test.go: a valid multi-type file exercising shared strings, an inline string, a numeric cell, a real XLSX boolean cell, and sparse/gapped columns; a missing-required-column file-level error; ParseImportFile's dispatch on both a plain CSV byte stream and a built .xlsx) and a new "clean xlsx file goes ready" subtest in the existing live-Postgres TestValidateImportFlow integration suite, proving the real worker dispatch path end to end. Live-verified against a fresh docker-compose stack with a real Chromium: a hand-built real .xlsx file (Python's stdlib zipfile, matching the exact shared-strings/worksheet XML shape a real spreadsheet app writes) was uploaded through the actual browser UI and resolved to "2 questions validated and ready to add" - proving the full path (browser upload -> HTTP -> local storage -> worker -> ParseImportFile -> ParseImportXLSX -> ready status -> UI poll) for real, not just at the unit level; the existing CSV-only web/e2e/import.e2e.mjs suite was re-run against the same stack and still passes unchanged (8/8). A drive-by fix from the same audit: web/src/index.css's .toast rule used a raw `color: #fff` instead of the existing `--color-surface` token (the established on-dark-background text convention, per .button-primary's own use of the same token) - tokens.css and docs/11 line 179 both state components must never use a raw hex value; fixed, zero visual change since --color-surface is also #ffffff. Full Go test suite (go build/vet/test against live Postgres, all packages), gofmt, golangci-lint (0 issues), and npm typecheck/lint/build all pass with no regressions.

In short: the server-authoritative core, the web attempt player (including its client-side guardrail enforcement), realtime end to end - teacher-facing monitor, the attempt:{id} and user:{id}:notify student channels, quiz.extended/closed publishing, the kick-reason lockout screen/banners, single-active-session enforcement, in-app plus email assignment notifications, current_question wiring, and now heartbeat/disconnect tracking too (see Milestone 5) - guardrails/kick (both backend and client, including per-teacher rate limiting, and now the kicked-attempt score-override endpoint/UI too - see Milestone 8), bulk import (now including its production R2 object store and, per the template doc's original "CSV/XLSX" wording, XLSX upload support - see the new entry above), analytics (now including per-quiz percentile, see Milestone 8), the admin console, and all of telemetry export (both processes, now including checked-in Grafana Cloud dashboard/alert-rule JSON) are all done and solid. The go-live-herd load test now exists too (scripts/loadtest/{herd.js,run.sh} plus `macquiz loadtest-seed`, see Milestone 9 above) and, in building it, found and fixed a real unbounded-DB-connection-pool bug it was designed to catch. A prior pass's claim that every product-functionality gap was closed turned out to be incomplete: a fresh doc-vs-code audit found docs/07's "score and percentile per quiz" was never built (fixed two iterations ago, see Milestone 8) and docs/06 line 80's "teacher can override a kicked attempt's score to zero" had no endpoint or UI (now fixed too, this iteration - see Milestone 8's second paragraph). An earlier iteration also found and fixed a genuine test flake (not a production bug): server/internal/quiz/schedulerflow_test.go's exam-day-backup subtest used window(1h, 2h)-scale offsets to assert "starts today", which silently breaks whenever the suite runs within roughly the last hour of the UTC day (starts_at rolls past midnight UTC); it now uses minute-scale offsets. With the score-override gap closed, every named product-functionality gap in docs/01-10 and docs/12 is done; what's left there is genuinely operational infrastructure work, not product functionality: running the load test at its full 1,000/2,000 scale against representative target-sized infrastructure rather than a shared dev sandbox, live-importing/verifying the new Grafana dashboard/alert JSON against an actual provisioned Grafana Cloud stack, and (if ever wanted) a host-metrics agent plus a backup dead-man's-switch ping for the two docs/10 section 3 thresholds that have no app-emitted metric behind them (disk usage, backup-job success) - none of these are achievable inside this sandbox, which has no real Grafana Cloud/production-scale infrastructure to target. A fresh audit against docs/08 (security), docs/11 (frontend design system), docs/03 (data model), and docs/04 (API) - deliberately picking sections that hadn't been recent iterations' specific focus, since two prior passes had already found real gaps this way - found one more real, previously-unflagged product gap and fixed it this iteration: docs/08 section 4 and docs/04 section 5 both named "rate limits on answer saves per attempt" as a requirement, but PUT /attempts/:id/answers/:qid had no server-side limiter at all (see the new entry above). That same audit found three more real gaps against docs/11-frontend-design-system.md specifically (missing Toast component, kick/readmit/override-score using window.prompt() instead of the documented destructive-flow modal, and a missing leaderboard/"dark island" component whose status as a true FR vs. stray mockup artifact is unclear); the Toast component and the destructive-flow modal (a new shared DestructiveConfirmModal.tsx, wired into all three window.prompt() call sites - kick, readmit, and score override) are now DONE, fixed across the two most recent iterations - see the new entry above for detail. Only the leaderboard/"dark island" gap remains open, deliberately, pending a scope call on whether it is a real FR. Everything else in docs/03/04/08 checked out against the actual code (auth TTLs, argon2id params, refresh-token rotation/reuse detection, forced password reset, the can() policy, 404-not-403 existence hiding, answer-key stripping, kick/readmit reason+rate-limit, audit_log/attempt_events append-only triggers, every documented data-model table/column/index, every documented API route). The one remaining known gap from the docs/02/09/12 audit - oapi-codegen not actually being used for the Go server - is now fully closed: Go request/response types generate from api/openapi.yaml, CI enforces they stay in sync, and all seven named handler/service response types (analytics.QuizStats/StudentStats/TeacherStats/OrgStats, quiz.ResultRow, quiz.LiveRow, and now attempt.Attempt/Result) actually build their responses from the generated types, with the float32-vs-float64 precision question deliberately resolved (not deferred) each time. attempt.Attempt/Result was the last and previously the most-deferred item, scoped by several prior iterations as materially larger/riskier than any package done so far; it turned out mechanical once actually attempted - see the new entry above. That "every named product-functionality gap in docs/01-10 and docs/12 is done" claim itself turned out to have one more blind spot: no prior iteration had cross-checked the original quiz-system-design.html SDD (as opposed to its docs/*.md distillation) against the code, and it found a real, previously-unflagged behavioral gap - live-quiz audience "late invite" was specified in the SDD but never implemented (the code did the opposite, freezing the audience entirely once live) - now fixed; see the new entry above. deployment-plan.html and high-level-architecture.html were also read in full this pass and confirmed to add nothing beyond what Milestone 9's entries already cover or pure SaaS/infra setup outside code scope.
