1. DONE - Results release policy + results views (backend and web player). Both the release-gated API and the web results UI are shipped.
2. DONE - The web attempt player, My quizzes workspace, and released-results review. Proven by web/e2e/player.e2e.mjs.
3. Remaining (minor, from the plan): the Redis snapshot cache for attempt start - Redis is wired for pub/sub events but nothing caches the question snapshot at Start yet.

- Milestone 5 - Realtime (FR-7): the roster snapshot, attempt_events source-of-truth log, Redis pub/sub publisher, progress coalescing, the WS monitor gateway, and the teacher live-monitor dashboard (LiveMonitorPanel) are all DONE. The attempt:{id} student-facing WS channel is now fully DONE end to end, client included: realtime.Gateway.handleAttempt (server/internal/realtime/gateway.go) authorizes the attempt's owning student only, subscribes to the same quiz:{quiz_id}:events stream as the monitor channel, relays a matching attempt.kicked envelope then force-closes the socket (docs/06 section 4 step 4), and passes through any quiz.* banner event untouched. quiz.extended/quiz.closed publish from quiz/lifecycle.go's Extend/ForceClose (proven by server/internal/quiz/publishflow_test.go). web/src/player/AttemptPlayer.tsx now opens `/ws/attempts/:id` while playing (reconnecting on drop, mirroring LiveMonitorPanel's pattern): an attempt.kicked push renders a reason-aware "You were removed from this quiz" lockout screen (the REST-fallback 409 ATTEMPT_KICKED path is unchanged as a backstop and now renders the same screen instead of a generic one), and quiz.extended/quiz.closed render a dismissable banner strip (extended also silently re-fetches the attempt detail so the countdown picks up any pulled-forward deadline_at). Verified live against a running docker-compose stack in a real Chromium: web/e2e/livemonitor.e2e.mjs was updated (its kicked-student assertion now expects the socket-delivered reason screen, arriving well before the old REST-fallback path would have fired) and passes; an ad-hoc extend/close banner run (not checked in) also confirmed both banners render and dismiss correctly. Remaining:
  - the user:{id}:notify channel - no implementation exists (doc-comment only)
  - heartbeat/disconnect tracking (attempt.disconnected/reconnected events) - the channel carries no inbound heartbeat handling yet, so the dashboard still cannot show "disconnected". Note: implementing this requires reworking realtime.Gateway.handleAttempt away from coder/websocket's CloseRead (which force-closes on any inbound data frame with StatusPolicyViolation) to a real read loop, since a client-sent heartbeat is necessarily a data message - a bigger, riskier change than it first looks.
  - current_question wiring is now DONE: attempts.current_question (new migration 00007) stores the 1-based ordinal of the last question SaveAnswer resolved (the closest proxy REST autosave has to a navigation cursor); it rides the existing attempt.progress event/publish and the GET /quizzes/:id/live roster row, and LiveMonitorPanel's progress cell now shows "Q{n} · answered/total". Proven by server/internal/attempt/{eventflow,publishflow,liveflow}_test.go (updated from asserting explicit null to asserting the ordinal).
  - Archive does not publish a quiz.* event, but this is not a gap - docs/05 section 2's vocabulary only lists quiz.extended/quiz.closed, not an archive banner

- Milestone 6 - Guardrails and kick (FR-10, FR-11): the backend is DONE - kick, readmit, violation reporting with count/warn/off policies, the auto_submit violation ladder, and per-teacher rate limiting on kick/readmit (server/internal/ratelimit, shared with login), all proven by E2E suites. The client side is also now DONE: web/src/player/AttemptPlayer.tsx implements fullscreen lock, focus/blur tracking, clipboard/context-menu blocking, and reports to POST /attempts/:id/events. The student lockout screen showing the kick reason and the quiz.extended/close/kick banners - this section's last remaining item - are now DONE too; see Milestone 5.

- Single active session per attempt (docs/08 section 1, docs/06 section 3): now DONE. server/internal/realtime/gateway.go tracks the one open attempt:{id} socket per attempt ID; a second device's connection force-closes the first with WebSocket close code 4001 (statusSessionReplaced) and calls the new SessionInvalidatedFunc callback, wired in main.go to attempt.Service.LogSessionInvalidated - a new attempt_events row (attempt.session_invalidated) is appended and published on the quiz channel, satisfying "logged as an event the teacher can see" (docs/08 section 1). web/src/player/AttemptPlayer.tsx's attempt socket recognizes close code 4001 and shows a "This attempt was opened in another window or device" banner instead of reconnecting (reconnecting would just re-invalidate the newer session back, an endless loop). Verified with gateway unit tests (server/internal/realtime/gateway_session_test.go), a service-level itest (server/internal/attempt/sessioninvalidateflow_test.go), and an ad-hoc two-browser-context Puppeteer run against the live docker-compose stack (not checked in) confirming the stale tab shows the banner while the newer tab keeps working normally.

- Milestone 7 - Bulk import (FR-3): DONE. server/internal/quiz/{importvalidate,importworker,importstorage}.go implement the full pipeline - template parse and validation, upload registration, async validate worker job, review UI with poll/error reporting, and a transactional commit. Remaining: the object storage is LocalImportStorage (plain files on disk) standing in for the documented production object store; a real R2/S3-backed ImportStorage still needs to be written before this is production-deployable at scale.

- Milestone 8 - Analytics (FR-8, FR-9): DONE. server/internal/analytics/{rollup,service,http}.go implement the rollup-on-close job (quiz_stats/student_stats: distribution, item analysis, integrity signals), the quiz/student/teacher/org analytics endpoints, CSV result export, the teacher QuizStatsPanel, and the admin OrgStatsPanel. No known remaining gaps here.

- Milestone 9 - Production hardening: mostly DONE - docker-compose.prod.yml, Caddyfile, .env.production.example, the nightly pg_dump-to-R2 backup cron plus the exam-day pre-window trigger, OpenTelemetry metrics export from both processes (server/internal/telemetry), the /healthz DB+Redis+queue-lag check, /deploy-check, migrate-on-boot, and a deploy.yml GitHub Action gated on live-quiz status. Worker-process telemetry export is now DONE: worker.Run (server/internal/worker/worker.go) calls telemetry.Setup(ctx, cfg, "macquiz-worker") and registers the same macquiz.queue.lag_seconds gauge serve() does (querying its own DB handle, so lag exports even if the API process is down), plus a new macquiz.worker.due_transitions counter labeled by kind (quizzes_opened, quizzes_closed, attempts_auto_submitted, attempts_force_submitted, attempts_graded, results_released, quizzes_rolled_up) recorded once per sweepDue pass with any nonzero transitions - mirroring the fields already in its "due transitions applied" log line. Verified with a telemetry unit test assertion and the existing DB-backed TestWorkerOpensAndClosesQuiz, which now exercises Setup/RegisterQueueLagGauge/RecordDueTransitions end to end against a real river/Postgres run. Remaining:
  - the 1,000-student go-live load test (k6/artillery or similar) - no load-test scripts exist anywhere in the repo yet
  - a restore-drill script/automation (docs call for pulling the latest dump into a scratch container once per term and smoke-testing it) - backups are written but nothing verifies they restore
  - actual Grafana Cloud dashboard/alert-rule definitions - thresholds are documented but not checked into the repo as dashboard/alert JSON (may be an intentionally manual ops task, low confidence this is a real gap)

Smaller design-doc items with no home yet

- Admin console UI: DONE - web/src/admin/{AdminWorkspace,UsersPanel,GroupsPanel,OrgStatsPanel}.tsx, no longer a placeholder.
- Audit log read side: DONE - GET /api/v1/audit, keyset-paginated, admin-only.
- Live-quiz teacher controls (extend, force-close, archive): DONE, including all the read-path widening for the archived terminal state, and now also the quiz.extended/quiz.closed realtime publish and the client rendering it as a banner (see Milestone 5). No known remaining gap here.
- Notifications on assignment changes (in-app/email): still NOT implemented - no email/SMTP code anywhere in the repo, and no in-app notification delivery beyond the doc-stub user:{id}:notify channel.
- Single active session per attempt: now DONE - see the new entry under Milestone 6 above.

In short: the server-authoritative core, the web attempt player (including its client-side guardrail enforcement), realtime's teacher-facing half plus the attempt:{id} student channel end to end (server and client, including quiz.extended/closed publishing, the kick-reason lockout screen/banners, and single-active-session enforcement), guardrails/kick (both backend and client, including per-teacher rate limiting), bulk import, analytics, the admin console, and now all of telemetry export (both processes) are all done and solid. What's left clusters into two things: (1) the realtime loose ends that remain unbuilt - the user:{id}:notify channel and heartbeat/disconnect tracking (current_question wiring is now done, see Milestone 5); and (2) the last operational-maturity gaps - the load test, a restore drill, a real object store behind bulk import, and notification delivery.
