1. DONE - Results release policy + results views (backend and web player). Both the release-gated API and the web results UI are shipped.
2. DONE - The web attempt player, My quizzes workspace, and released-results review. Proven by web/e2e/player.e2e.mjs.
3. Remaining (minor, from the plan): the Redis snapshot cache for attempt start - Redis is wired for pub/sub events but nothing caches the question snapshot at Start yet.

- Milestone 5 - Realtime (FR-7): the roster snapshot, attempt_events source-of-truth log, Redis pub/sub publisher, progress coalescing, the WS monitor gateway, and the teacher live-monitor dashboard (LiveMonitorPanel) are all DONE. Remaining:
  - the attempt:{id} student-facing WS channel - no implementation exists (doc-comment only)
  - the user:{id}:notify channel - no implementation exists (doc-comment only)
  - heartbeat/disconnect tracking (attempt.disconnected/reconnected events)
  - wiring events into the live snapshot's current_question (still always null)

- Milestone 6 - Guardrails and kick (FR-10, FR-11): the entire backend is DONE - kick, readmit, violation reporting with count/warn/off policies, and the auto_submit violation ladder, all proven by E2E suites. Remaining:
  - the client side is entirely missing: web/src/player/AttemptPlayer.tsx has zero guardrail code today - no fullscreen lock, no focus/blur tracking, no clipboard blocking, and no calls to POST /attempts/:id/events. A student taking a quiz right now experiences none of the guardrails the backend enforces.
  - the student lockout screen (no UI component exists) and the quiz.extended/close/kick banners to students - both depend on the still-missing attempt:{id} socket
  - per-teacher rate-limiting on kick/readmit (today the only rate limiter in the codebase is on login)

- Milestone 7 - Bulk import (FR-3): DONE. server/internal/quiz/{importvalidate,importworker,importstorage}.go implement the full pipeline - template parse and validation, upload registration, async validate worker job, review UI with poll/error reporting, and a transactional commit. Remaining: the object storage is LocalImportStorage (plain files on disk) standing in for the documented production object store; a real R2/S3-backed ImportStorage still needs to be written before this is production-deployable at scale.

- Milestone 8 - Analytics (FR-8, FR-9): DONE. server/internal/analytics/{rollup,service,http}.go implement the rollup-on-close job (quiz_stats/student_stats: distribution, item analysis, integrity signals), the quiz/student/teacher/org analytics endpoints, CSV result export, the teacher QuizStatsPanel, and the admin OrgStatsPanel. No known remaining gaps here.

- Milestone 9 - Production hardening: mostly DONE - docker-compose.prod.yml, Caddyfile, .env.production.example, the nightly pg_dump-to-R2 backup cron plus the exam-day pre-window trigger, OpenTelemetry metrics export (server/internal/telemetry), the /healthz DB+Redis+queue-lag check, /deploy-check, migrate-on-boot, and a deploy.yml GitHub Action gated on live-quiz status. Remaining:
  - the 1,000-student go-live load test (k6/artillery or similar) - no load-test scripts exist anywhere in the repo yet
  - a restore-drill script/automation (docs call for pulling the latest dump into a scratch container once per term and smoke-testing it) - backups are written but nothing verifies they restore
  - worker-process telemetry export - metrics are wired into serve() only; the worker does not yet export metrics
  - actual Grafana Cloud dashboard/alert-rule definitions - thresholds are documented but not checked into the repo as dashboard/alert JSON (may be an intentionally manual ops task, low confidence this is a real gap)

Smaller design-doc items with no home yet

- Admin console UI: DONE - web/src/admin/{AdminWorkspace,UsersPanel,GroupsPanel,OrgStatsPanel}.tsx, no longer a placeholder.
- Audit log read side: DONE - GET /api/v1/audit, keyset-paginated, admin-only.
- Live-quiz teacher controls (extend, force-close, archive): DONE, including all the read-path widening for the archived terminal state. Remaining: only the student-facing quiz.extended/close/archive banners, deferred to the still-missing student WS.
- Notifications on assignment changes (in-app/email): still NOT implemented - no email/SMTP code anywhere in the repo, and no in-app notification delivery beyond the doc-stub user:{id}:notify channel.
- Single active session per attempt: still NOT implemented - depends on the missing attempt:{id} socket to detect and invalidate a second concurrent session.

In short: the server-authoritative core, the web attempt player, realtime's teacher-facing half, guardrails/kick's backend, bulk import, analytics, the admin console, and most of production hardening are all done and solid. What's left clusters into three things: (1) the still-unbuilt student-facing WebSocket channel (attempt:{id} + user:{id}:notify) and everything that depends on it - heartbeat tracking, current_question, single-session enforcement, and student-facing banners; (2) the client-side guardrail enforcement in the web attempt player, which today does not call the backend's violation-reporting endpoint at all; and (3) the last operational-maturity gaps - the load test, a restore drill, worker telemetry, a real object store behind bulk import, and notification delivery.
